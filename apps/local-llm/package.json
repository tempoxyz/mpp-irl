{
  "name": "local-llm",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env.local src/server.ts",
    "build": "tsc",
    "start": "node --env-file-if-exists=.env.local dist/server.js",
    "chat": "tsx src/scripts/chat.ts",
    "responses": "tsx src/scripts/responses.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "hono": "^4.12.32",
    "openai": "^6"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.23.1",
    "typescript": "^5",
    "vitest": "^3"
  }
}
