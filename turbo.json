{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["N2YO_API_KEY", "MPP_SECRET_KEY", "RECIPIENT_ADDRESS"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "outputs": [".next/**", "dist/**"]
    }
  }
}
